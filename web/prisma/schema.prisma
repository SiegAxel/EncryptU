generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model ContactTicket {
  id          Int      @id @default(autoincrement())
  firstName   String
  lastName    String
  email       String
  reason      String     // <- String, NO enum
  phone       String
  description String
  createdAt   DateTime @default(now())
}

enum Role {
  admin
  soporte
  usuario
}

model User {
  id           Int      @id @default(autoincrement())
  name         String
  email        String   @unique
  passwordHash String
  role         Role     @default(usuario)  // 👈 nuevo campo con default
  createdAt    DateTime @default(now())
}
